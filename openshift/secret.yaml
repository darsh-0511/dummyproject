apiVersion: v1
kind: Secret
metadata:
  name: blu-reserve-secret
  namespace: blu-reserve
  labels:
    app: blu-reserve
type: Opaque
stringData:
  # Database credentials (if needed in future)
  DB_USERNAME: "blu_admin"
  DB_PASSWORD: "changeme123"
  
  # API Keys (if needed)
  API_SECRET_KEY: "your-secret-key-here"
  
  # JWT Secret (if implementing real auth)
  JWT_SECRET: "your-jwt-secret-here"
